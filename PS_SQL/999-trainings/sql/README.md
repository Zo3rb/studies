# Native SQL Files

Store all raw `.sql` files here (init, migrations, reports, etc.).

For bootstrap and reset, use the shell launchers in `sql/init/bootstrap.sh` and `sql/init/reset.sh`, while the individual `sql/init/*.sql` files remain executable in dependency order from tools like DBeaver.
