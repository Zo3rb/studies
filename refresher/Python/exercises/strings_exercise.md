# exercises/strings_exercise.md

## Exercise – Strings Practice

Implement the four helper functions defined in the **Strings** lesson. Only provide the signatures and a `pass` statement; the implementations will be added later.

```python
def reverse_string(s: str) -> str:
    """Return the reverse of *s*.

    Args:
        s: Input string.
    Returns:
        Reversed string.
    """
    pass

def is_palindrome(s: str) -> bool:
    """Return ``True`` if *s* reads the same forward and backward (case‑insensitive, ignoring spaces).

    Args:
        s: Input string.
    Returns:
        ``True`` if palindrome, else ``False``.
    """
    pass

def count_vowels(s: str) -> int:
    """Count the number of vowel characters (a, e, i, o, u) in *s* (case‑insensitive)."""
    pass

def remove_whitespace(s: str) -> str:
    """Return *s* with **all** whitespace characters removed.

    Args:
        s: Input string.
    Returns:
        String without spaces, tabs, newlines.
    """
    pass
```

### Guidelines
- Keep the **type hints** exactly as shown.
- Do **not** import any modules inside the function bodies.
- Use `pass` to indicate the implementation is pending.
- Follow PEP‑8 indentation (4 spaces) and include a docstring for each function.

---
*The exercise follows the same layout as the previous lesson exercise files.*
